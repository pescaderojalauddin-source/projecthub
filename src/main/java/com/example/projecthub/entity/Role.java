package com.example.projecthub.entity;

/** Роль пользователя для разграничения прав (RBAC). */
public enum Role {
    /** Обычный пользователь. */
    USER,
    /** Администратор системы. */
    ADMIN
}
